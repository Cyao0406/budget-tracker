# 未來更新規劃

記錄之後版本要做的事。已經完成的功能請看 [CLAUDE.md](CLAUDE.md) 或 [README.md](README.md)，這份文件只放**還沒做**的。做完一項就打勾並簡短記一下完成日期/commit，不要整項刪掉——保留歷史比較看得出演進過程。

---

## 一、雲端同步版（多裝置同步）

目前是刻意的單機版（2026-08-07 專案剛開始時討論規格就決定先求簡單、不做雲端），這是之後要做「真的想兩台裝置都同步」時的完整規劃，動工前務必先跟使用者確認範圍，這是架構等級的改動，不是小功能。

- [ ] **選後端方案**：兩個候選都是免費額度夠用、不用自己維運伺服器——
  - Firebase（Firestore + Authentication）：生態成熟、文件多，免費額度對個人記帳量綽綽有餘
  - Supabase（Postgres + Auth）：開源、資料是關聯式的，之後想做更複雜的統計查詢會比較方便
  - 兩個都要「先登入才能用」，跟現在「打開就能用」的體驗不一樣，要想清楚要不要保留「不登入、純本機」的選項並存
- [ ] **登入機制**：Email/密碼最簡單；或用 Google/Apple 登入省得自己存密碼
- [ ] **資料模型加 userId**：records / categories 都要能歸屬到使用者，雲端規則要擋掉別人讀別人的帳
- [ ] **離線優先**：PWA 的核心價值就是斷網也能用，雲端版**不能**變成「沒網路就打不開」——本地先寫 localStorage，背景再同步雲端，斷線時排隊，恢復連線後補送
- [ ] **衝突處理**：兩台裝置同時新增/編輯同一筆怎麼辦。最簡單先做 last-write-wait（後寫的贏，用 `updatedAt` 比大小），之後真的常衝突再考慮更複雜的合併邏輯
- [ ] **既有資料遷移路徑**：現在已經有人（包含目前這個使用者自己）在用單機版存了真實資料，不能做成「升級後資料就沒了」——需要「第一次登入時，把本機 localStorage 一次性上傳」的流程
- [ ] **多裝置管理 UI**：至少要能看到「目前登入哪個帳號」「登出」，之後可加「查看已連結裝置」

---

## 二、其他建議功能（依建議優先順序，非強制）

1. - [ ] **固定支出/訂閱提醒**：仿 MoneyNote 的 fixedCostSettings，設定「每月房租/Netflix」，到期自動生成紀錄或至少提醒
2. - [ ] **分類預算上限**：每個分類可設月預算，超支時圖表/列表要有警示（顏色或圖示），呼應 MoneyNote 匯出檔裡本來就有的 `#BUDGET_SETTINGS` 概念
3. - [ ] **月趨勢圖**：現在的圓餅圖只能看單一期間的佔比，看不出「這個月比上個月多花多少」，建議加一個長條圖比較近 6-12 個月的支出/收入趨勢
4. - [ ] **刪除復原（undo）**：目前刪除紀錄/分類要靠 `window.confirm()` 防手滑，可以加一個「已刪除，復原」的短暫 toast，容錯率更高
5. - [ ] **多帳戶/多本帳**：現金、信用卡、銀行帳戶分開記，呼應 MoneyNote 的 `accountingId` 概念；目前所有紀錄都是同一本帳
6. - [ ] **記帳提醒通知**：用 PWA 的 Notification API，定時提醒「今天還沒記帳」
7. - [ ] **App 鎖**：記帳資料算隱私財務資訊，可加簡單的密碼/生物辨識鎖（Web Authentication API 或簡易 PIN）
8. - [ ] **分類加 emoji 圖示**：現在分類只有顏色圓點辨識，列表項目多了之後光靠顏色不夠好認，可以讓使用者選一個 emoji 當圖示（MoneyNote 匯出檔裡的 `icon` 欄位其實已經有這個概念，只是我們匯入時沒有用到）
9. - [ ] **搜尋/篩選紀錄**：依關鍵字、金額範圍、分類篩選歷史紀錄，資料量大了之後（現在已經有 1000+ 筆）用日/週/月分頁會不夠用
10. - [ ] **PDF 報表匯出**：現在只有 CSV，想直接列印或分享報表時 PDF 比較實用
11. - [ ] **iOS 主畫面 Widget**：桌面小工具直接顯示本月支出/快速記一筆，不用開 App
12. - [ ] **語音輸入**：對著手機講「早餐全家一百五」直接解析成一筆紀錄

---

## 三、技術債／內部改善（不影響使用者體感，但值得做）

- [ ] **CSV 匯入格式做成可擴充架構**：現在 `importMoneyNoteCsv()` 是寫死認 MoneyNote 格式，之後如果還要支援別的記帳 App（AndroMoney、CWMoney 等）建議抽成「格式偵測 + parser 註冊」的架構，不要每次都複製一份新函式
- [ ] **基本自動化測試**：目前所有驗證都是手動用 `javascript_tool` 操作 DOM 完成，沒有留下可重跑的測試檔。功能持續變多之後建議至少把核心的 `parseQuickInput`／`guessCategory`／CSV 解析邏輯抽出來寫成幾個純函式單元測試（不需要引入完整測試框架，一個簡單的 assert 腳本也夠用）
- [ ] **Service worker cache 版本化**：`sw.js` 的 `CACHE_NAME` 目前是手動改字串才會強制更新快取，可以自動化（例如用 build 時間戳）避免使用者卡在舊版本
- [ ] **色彩系統可及性複查**：`--pastel-*` 這組是使用者自選的裝飾色，沒有跑過完整的 CVD 驗證（`--series-*` 那組有），純色票夠用但如果之後圖表也要用到 pastel 色，要先過一次驗證
