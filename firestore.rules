rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每個使用者只能讀寫自己 uid 底下的資料，不同帳號互相看不到彼此的記帳紀錄
    match /users/{uid}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
