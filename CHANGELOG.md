# 版本更新說明

給使用者看的白話版更新紀錄（技術細節請看 [CLAUDE.md](CLAUDE.md)）。版本號規則：大版號＝重要新功能，小版號＝改善或修正。App 內「設定」裡也看得到最近幾筆，這份文件放完整歷史。

---

## v2.7 — 開場載入畫面、日曆格線、報表月份篩選、搜尋進階條件（2026-08-13）

- App 開啟時新增載入畫面，確認是最新版本才會進入
- 日曆分頁的月曆格子加上格線設計，區隔每一天
- 分類 drill-down 頁的近 6 個月趨勢長條圖可以點選單一月份，只看該月的紀錄
- 紀錄列表的日期分組改成色塊樣式，顯示當天收支淨額，配色跟主題反過來（暖灰底＋金色邊框，不會刺眼）
- 日曆分頁搜尋新增進階篩選：金額區間、日期區間，可以跟關鍵字一起用

## v2.6 — 介面比照參考截圖、修正分類重複問題（2026-08-13）

- 報表分頁改成「月度報告／年度報告」（取代原本的日/週/月），可以往前後切換月份或年度
- 輸入分頁「進階選項」的分類改成直接點格子選，不用再打開下拉選單
- 日曆分頁的星期標題、月份導覽列、下方收支合計比照參考畫面重新設計
- 報表分頁的分類列表加上分隔線，跟圓餅圖區塊分開，看起來更清楚
- 金額可以輸入 0 了
- 修正雲端登入後分類會重複出現的問題（同一個分類變成兩筆，一筆有圖示一筆沒有）；設定裡新增「清理重複分類」按鈕，可以一次把既有的重複分類清乾淨
- 刪除分類新增「不指定分類」選項：如果這個分類底下本來就沒有紀錄（例如手滑多新增的），可以直接刪除不用被迫選一個轉移對象
- 設定裡新增「重新套用關鍵字分類」工具：新增或修改分類的關鍵字之後，可以用這個工具重新檢查舊紀錄要不要跟著改分類
- 修正刪除分類時，跳出的「轉移到哪個分類」選單會被設定面板蓋住看不到的問題

## v2.5 — 介面拆成三個頁面（2026-08-13）

- 主畫面拆成「輸入／日曆／報表」三個分頁，底部新增分頁列可以切換
- 日曆分頁全新設計：整個月的格子直接顯示每天的收支金額，點某一天可以只看那天的紀錄，下方也有搜尋框
- 報表分頁的圓餅圖改成點一下跳出金額泡泡（不會整頁篩選），點分類列表會另外開一頁看那個分類近 6 個月的趨勢長條圖和逐筆紀錄
- 一行文字快速記帳（例如「早餐 全家 150」）維持不變，還是輸入分頁的主要記帳方式

## v2.4 — 修正與微調（2026-08-12）

- 修正登入雲端同步後，分類 emoji 圖示會消失的問題
- 新增「訂閱費」分類，跟娛樂費分開（Netflix、Spotify、Claude、Cursor、健身房會費等訂閱制支出）
- 刪除紀錄新增輕微震動回饋（iOS Safari 不支援震動，其他平台會有感覺）
- 搜尋紀錄時，不論目前選的是日/週/月分頁都會顯示每筆紀錄的日期，日期文字也調大、調亮方便閱讀

## v2.3 — 五項小功能更新（2026-08-12）

- 紀錄列表顯示記帳時間，編輯紀錄時可以手動調整（例如隔天才補記，可以改回實際發生的時間）
- 分類可以加一個 emoji 圖示，列表項目更好辨認
- 刪除紀錄改成「已刪除」提示 + 復原按鈕，取代原本要再按一次確認的彈窗
- 紀錄列表新增搜尋框，可以用分類、備註關鍵字或金額找到任何一筆紀錄，不限目前選的日/週/月
- 圓餅圖或圖例可以點選單一分類篩選，只看該分類的金額與紀錄，再點一次或按「清除篩選」恢復

## v2.2 — 介面改成金色主題（2026-08-12）

- 按鈕、選中狀態、連結文字等介面強調色從藍色改成呼應主畫面圖示的金色
- 這個改動跟「分類顏色」是分開的：分類自己選的顏色不會被動到，只是原本共用同一組藍色的介面元件現在改用獨立的金色主題色

## v2.1 — 圖示改版（2026-08-12）

- 主畫面圖示重新設計：先改成呼應圓餅圖報表的甜甜圈圖樣式，後來依需求改成金幣造型的 `$` 符號圖示

## v2.0 — 雲端同步上線（2026-08-12）

- 新增「登入 Google 帳號」選用功能，登入後資料會自動同步到雲端，換手機、加裝置都不怕資料不見
- 不登入完全不受影響，單機模式一樣正常使用
- 正式站改用 Firebase Hosting（`https://budget-tracker-8edd1.firebaseapp.com/`），比原本的 GitHub Pages 更適合搭配雲端登入功能
- 離線快取邏輯改成「有網路優先拿最新版本」，之後更新功能只要關掉 App 重開就會是最新版，不用再重新加主畫面圖示

## v1.2 — 支援匯入舊記帳 App 資料（2026-08-07）

- 「匯入 CSV」可以直接讀取 MoneyNote App 匯出的資料，自動判斷分類、金額、日期
- 匯入時同性質的分類會自動合併（例如「醫療費」併進「醫療」），不會產生一堆重複分類
- 分類管理新增「合併刪除」：刪除分類時可以選要把紀錄轉移到哪一個分類，不是固定丟進「其他」

## v1.1 — 分類顏色更多選擇（2026-08-07）

- 分類顏色從 8 種增加到 16 種，新增一組淺色系選項，色彩分布更平均、更好分辨

## v1.0 — 記帳 App 上線（2026-08-07）

- 快速輸入一行文字（例如「早餐 全家 150」）自動判斷金額並猜測分類
- 日 / 週 / 月報表，圓餅圖 + 圖例呈現各分類佔比
- 點選日期列或月曆挑日期記帳
- 紀錄可以點開編輯或刪除
- 分類可自訂名稱、顏色、自動判斷用的關鍵字
- CSV 匯出，作為備份或搬家用
- 支援加入手機主畫面，像 App 一樣使用（PWA）
