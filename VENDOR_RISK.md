# 第三方服務風險與退場計畫

## 這份文件在講什麼

這個 App 的程式碼 100% 在你自己的 GitHub repo，你完全擁有、隨時可以搬走。但雲端同步功能依賴 **Firebase**（Google 的服務）——條款、定價、免費額度都是 Google 決定，不是你能控制的。這份文件記錄：**如果哪天 Firebase 的政策對你不利，你有哪些選項、該怎麼做**，不用等事情真的發生才臨時想辦法。

---

## 一、目前實際的風險有多大

- 現在用 Blaze 方案，但實際用量遠低於免費額度（Firestore/Auth/Hosting 都在 Spark 等級的免費額度內），除非用量暴增到超過（以家庭記帳的規模幾乎不可能），否則不會真的被收費。
- 這不是「訂閱制隨時漲價」那種風險，比較接近「Google 未來調整免費層級定義」的風險——這種事在 Google 其他服務上發生過，機率不是零，但也不是每年都會發生的事，不用過度緊張。
- 現實中更常發生的反而是：帳單警示沒設定，因為程式碼寫錯（例如迴圈漏加終止條件）或帳號被盜用，意外超出免費額度——這個風險比「Google 改政策」高得多，也是**現在就能處理**的部分，見下面第四節。

## 二、真正不會被鎖住的部分

- **程式碼**：完全在你的 GitHub repo，`app.js`/`index.html`/`style.css` 都是標準 vanilla JS，沒有任何 Firebase 專屬語法把你綁死。跟 Firebase 相關的只有兩個檔案：`firebase-config.js`（初始化設定）跟 `firestore.rules`（安全規則）——換後端是重寫這兩個檔案，不是整個 App 重寫。
- **資料，有三層備份，不只依賴 Firebase：**
  1. **本機 localStorage**：不管有沒有登入都是即時的完整備份，就算 Firebase 明天整個消失，手機/電腦上現有的記帳資料本來就在，不會不見。
  2. **CSV 匯出**：App 內建功能，建議重大變動前（例如清除資料、大量匯入前）順手匯出一次。
  3. **Firestore 整包匯出**：用 Firebase CLI 的 `firebase firestore:export`，可以把雲端資料庫整包備份成檔案，不透過這個 App 也讀得到。

## 三、如果 Firebase 政策真的變動，依嚴重程度怎麼辦

1. **只是溫和漲價，還在可接受範圍**：不用做任何事，正常付費即可——目前用量本來就很小，漲價對每月實際花費的影響有限。
2. **免費額度大幅縮水，但服務還在**：先確認新的免費額度夠不夠用（記帳 App 用量小，多半還是夠），不夠再評估付費是否划算，跟下面選項 3 比成本。
3. **想完全脫離 Firebase**（不管是政策關係還是單純想換）：
   - **退回純單機模式（成本最低，隨時可做）**：把雲端登入功能整個關掉（`firebase-config.js` 不載入即可），App 退回最初的 localStorage-only 版本。功能不會缺——雲端同步本來就是加分項，不是必要條件，這是設計時就定下的原則（見 [CLAUDE.md](CLAUDE.md)）。
   - **換一個功能相近的後端**：Firebase Auth + Firestore 本質上是「帳號登入 + NoSQL 文件資料庫」這種泛用服務，市面上有幾個可以直接替換的選項：
     - **Supabase**：PostgreSQL-based，內建 Auth + Realtime，社群大、文件完整
     - **Appwrite**：開源，功能組合跟 Firebase 很接近
     - **Pocketbase**：單一執行檔可自架，零外部依賴，適合想完全掌控基礎設施的情況
   - 雲端同步的邏輯集中在 `app.js` 裡幾個函式（`diffAndPush`、`startCloudListeners`、`handleSignedIn` 等，細節見 [CLAUDE.md](CLAUDE.md)「雲端同步架構」一節），換後端主要是重寫這幾個函式跟 `firebase-config.js`，UI 跟記帳邏輯本身完全不用動。

## 四、平常就能做、幾乎零成本的保險（建議現在就去做）

- [ ] 到 Google Cloud Console／Firebase Console 設定「預算警示」（Budget Alerts），接近免費額度時會收到 email 通知，不用等帳單寄來才發現超額
- [ ] 重大變動前（清除資料、大量匯入前）養成順手「匯出 CSV」的習慣
- [ ] 每隔半年左右，用 `firebase firestore:export` 把 Firestore 資料整包備份到本機一次，不只依賴 App 內的 CSV 匯出
- [ ] 每年抽空看一兩次 Firebase 官方定價頁面或公告，提早發現政策變動，不用天天盯

## 五、真的要說一句底線

就算最壞情況發生（Firebase 明天全面收費、服務關閉、帳號被鎖），你會失去的頂多是「雲端同步」這個加分功能本身，不會失去：

- 程式碼（在 GitHub）
- 已經匯出過的資料（CSV／Firestore 備份）
- 手機/電腦上現有的記帳資料（localStorage，不受雲端服務狀態影響）

「雲端同步是選用、不是必要」是這個 App 從一開始就定下的設計原則（見 [CLAUDE.md](CLAUDE.md)），這份文件只是把「萬一真的要退場」這件事具體寫下來，不用每次想到才臨時評估一次。
