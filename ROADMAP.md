# 未來更新規劃

記錄之後版本要做的事。已經完成的功能請看 [CLAUDE.md](CLAUDE.md) 或 [README.md](README.md)，這份文件只放**還沒做**的。做完一項就打勾並簡短記一下完成日期/commit，不要整項刪掉——保留歷史比較看得出演進過程。

---

## 一、雲端同步版（多裝置同步）

✅ **已實作（2026-08-12）**，選用功能，不登入完全不影響單機使用。技術細節、資料流、已知風險見 [CLAUDE.md](CLAUDE.md)「雲端同步架構」一節。

- [x] **選後端方案**：Firebase（Firestore + Authentication）。原因：Spark 免費方案沒有 Supabase 那種「閒置 7 天自動暫停」機制，維護負擔較低；免費額度（每日 2 萬次寫入/5 萬次讀取）對個人記帳量綽綽有餘。保留「不登入、純本機」選項並存，登入只是加分項不是必要條件。
- [x] **登入機制**：Google 帳號登入（`signInWithPopup` + `GoogleAuthProvider`）。
- [x] **資料模型加 userId**：改用 `users/{uid}/records`、`users/{uid}/categories` 子集合，`firestore.rules` 擋掉跨帳號讀寫。
- [x] **離線優先**：localStorage 永遠是畫面即時來源；Firestore 的 `enableIndexedDbPersistence` 提供離線佇列，斷線時操作照常，恢復連線自動補送。
- [x] **衝突處理**：Firestore 內建文件層級 last-write-wins；每筆記帳紀錄本來就是獨立文件，不同裝置新增不同紀錄不會互相覆蓋，只有「同一筆在兩台裝置都離線時被改到」才可能其中一次被覆蓋。
- [x] **既有資料遷移路徑**：登入時偵測「雲端全空 + 本機有資料」才觸發搬遷提示，確認寫入成功才視為完成，過程不清除本機資料。
- [x] **多裝置管理 UI**：設定裡有「已登入：{email}」狀態顯示 + 登出按鈕（基本款，沒有做「查看已連結裝置」列表，目前用不太到）。
- [x] **建立真實 Firebase 專案**：`budget-tracker-8edd1`，已部署在 Firebase Hosting（`https://budget-tracker-8edd1.firebaseapp.com/`，取代原本的 GitHub Pages 網址）。過程踩了不少登入相關的坑，細節都記在 [CLAUDE.md](CLAUDE.md) 的「已知眉角」。

---

## 二、其他建議功能（依建議優先順序，非強制）

1. - [ ] **時間記錄功能**（使用者 2026-08-12 提出）：記帳時自動記下當下的「時:分」，例如 8:30 輸入「早餐 150」，之後在紀錄列表上會看到這筆是 8:30 記的。這個時間之後要能**手動調整**——常見情境是「今天忘記記帳，隔天才補記」，這時候要能把時間改成實際發生的時間，而不是永遠卡在補記當下的時間。技術上 `records` 資料已經有 `createdAt` 欄位（目前只拿來做同一天內紀錄的排序，畫面上沒顯示、`editRecordSheet` 也不能改），這個功能主要是：(a) 紀錄列表每筆顯示時間 (b) 編輯紀錄的畫面加一個時間欄位可以調整。
2. - [ ] **圓餅圖可點選篩選**（使用者 2026-08-12 提出）：點圓餅圖或圖例上的特定分類（例如「餐飲」區塊），畫面只顯示該分類的花費金額，紀錄列表也只列出該分類底下的紀錄，其他分類先隱藏起來；再點一次同一塊或提供一個「清除篩選」的方式可以取消，恢復顯示全部分類。
3. - [ ] **固定支出/訂閱提醒**：仿 MoneyNote 的 fixedCostSettings，設定「每月房租/Netflix」，到期自動生成紀錄或至少提醒
4. - [ ] **分類預算上限**：每個分類可設月預算，超支時圖表/列表要有警示（顏色或圖示），呼應 MoneyNote 匯出檔裡本來就有的 `#BUDGET_SETTINGS` 概念
5. - [ ] **月趨勢圖**：現在的圓餅圖只能看單一期間的佔比，看不出「這個月比上個月多花多少」，建議加一個長條圖比較近 6-12 個月的支出/收入趨勢
6. - [ ] **刪除復原（undo）**：目前刪除紀錄/分類要靠 `window.confirm()` 防手滑，可以加一個「已刪除，復原」的短暫 toast，容錯率更高
7. - [ ] **多帳戶/多本帳**：現金、信用卡、銀行帳戶分開記，呼應 MoneyNote 的 `accountingId` 概念；目前所有紀錄都是同一本帳
8. - [ ] **記帳提醒通知**：用 PWA 的 Notification API，定時提醒「今天還沒記帳」
9. - [ ] **App 鎖**：記帳資料算隱私財務資訊，可加簡單的密碼/生物辨識鎖（Web Authentication API 或簡易 PIN）
10. - [ ] **分類加 emoji 圖示**：現在分類只有顏色圓點辨識，列表項目多了之後光靠顏色不夠好認，可以讓使用者選一個 emoji 當圖示（MoneyNote 匯出檔裡的 `icon` 欄位其實已經有這個概念，只是我們匯入時沒有用到）
11. - [ ] **搜尋/篩選紀錄**：依關鍵字、金額範圍、分類篩選歷史紀錄，資料量大了之後（現在已經有 1000+ 筆）用日/週/月分頁會不夠用
12. - [ ] **PDF 報表匯出**：現在只有 CSV，想直接列印或分享報表時 PDF 比較實用
13. - [ ] **多幣別支援**（使用者 2026-08-12 核准加入）：記帳時可選擇幣別（例如出國旅遊時記外幣花費），依匯率換算成主要幣別呈現在報表/總計裡，但紀錄本身保留原始幣別跟原始金額，方便之後回頭查閱
14. - [ ] **資產/淨值管理**（使用者 2026-08-12 核准加入）：新增「資產」總覽，使用者可自訂多個具名子帳戶各自計入金額（例如「郵局帳戶：50,000」「股票資產：100,000」「現金：5,000」），總覽顯示目前總淨值，可手動更新各帳戶餘額；資料模型跟日常收支流水帳分開，屬於「某個時間點的餘額快照」而非逐筆交易
15. - [ ] **標籤功能**（使用者 2026-08-12 核准加入）：紀錄除了既有的單一「分類」外，可以再掛一個以上的自訂標籤（例如「出差」「旅遊」「必要支出」），標籤是跨分類的彈性篩選/統計維度，用途是補分類的不足（分類是互斥的單選，標籤可以多選並存）

> **已移除的構想**：iOS 主畫面 Widget、語音輸入（2026-08-12 移除）。iOS Widget 對純 PWA（沒有原生 App 外殼）來說是平台限制，技術上做不到，不是工夫問題；語音輸入靠的 Web Speech API 在 iOS Safari 支援度很差，使用者主要用手機，實際能用機率偏低。都不是「以後也許能做」，直接從清單拿掉，不留在「還沒做」的清單裡誤導後續規劃。

---

## 三、⚠️ AI 智慧功能（需串接第三方 AI API，資安/風險較高，跟其他項目分開評估）

跟上面「二、其他建議功能」不一樣的地方：這兩項都需要記帳資料離開 Firebase 生態系、送到第三方 AI 服務（Anthropic Claude API）處理，不是單純加個 UI 功能而已。額外花費評估過，個人使用或 5 人家庭共用一個月都在 NT$10～30 內，不是問題；真正要考慮的是資料怎麼送出去、金鑰怎麼保管。**開始實作前，下面「實作前必須做到的事」要先全部確認過，不要先把功能做出來再回頭補資安。**

- [ ] **AI 自動分類**（使用者 2026-08-12 提出構想，成本已評估）：記帳時除了現有的關鍵字比對，遇到關鍵字判斷不出來的情況（新店家、還沒設關鍵字的分類），改用 AI 判斷這筆備註最可能屬於哪個分類，用意是取代/輔助現有的 `guessCategory()`，不是額外一個獨立功能。
- [ ] **AI 消費洞察/分析評論**（使用者 2026-08-12 提出構想，成本已評估）：依日/週/月報表資料，用 AI 產生一段白話的消費分析評論（例如「這個月餐飲支出比上月增加 15%，主要集中在宵夜」），用意是幫使用者一眼看懂報表数字背後的意義，不是取代圖表本身。

**實作前必須做到的事：**

1. **API 金鑰只能存在 Cloud Functions 的環境變數裡，前端程式碼（`app.js`／瀏覽器）絕對不能出現金鑰本身。** 這是唯一能避免金鑰被打開瀏覽器開發者工具的任何人看走的做法，跟現有 `firebase-config.js` 裡 apiKey 可以公開的邏輯不一樣——AI API 金鑰是真正需要保密的密碼等級資訊，不能套用「反正有 rules 擋」那套。
2. **Cloud Functions 端點要先驗證呼叫者是合法登入使用者才處理請求。** 進來的請求要驗證 Firebase Auth 的 ID token，不能是任何人都打得到的公開端點——否則等於把你的 AI API 額度變成一個公開資源，被任何知道網址的人盜用暴刷。
3. **送給 AI 的資料要精簡到「這個功能真的用得到的最少欄位」：**
   - 可以送：分類、金額、日期——AI 判斷分類或算消費趨勢都用得到這些。
   - 備註欄位（店家名稱、品項）本身是最容易透露個人消費習慣、甚至醫療/交際等相對私密細節的欄位。AI 自動分類確實需要備註文字才能判斷分類；但 AI 消費洞察多半只需要「分類+金額+日期」的統計數字就夠了，不一定要把逐筆備註文字全部送出去，能不送就不送。
   - **絕對不能送出去的內容**：使用者 email、Firebase uid、裝置資訊等任何直接綁定「這是誰」的識別欄位。Cloud Function 內部當然要用 uid 決定該撈誰的資料，但送到 Anthropic API 的 payload 裡不該出現這些身份資訊——AI 只需要知道「這筆消費長怎樣」，不需要知道「這是誰的消費」。
4. **家人各自獨立帳號，Cloud Function 要確保資料不會跨帳號外流。** 處理請求時務必依照請求者自己驗證過的 uid 去撈資料，絕對不能讓任何一個使用者的請求意外撈到或送出別人的資料——這點要跟現有 `firestore.rules` 的隔離邏輯保持一致。
5. **功能預設關閉，使用者自己決定要不要開。** 尤其家人共用的情境，不是每個人都想要自己的消費紀錄被 AI 分析，這個開關要讓每個登入的使用者自己選擇，不能因為你開了大家就被迫一起用；啟用畫面上也建議附一行簡短說明「你的消費資料會送到第三方 AI 服務做分析」，讓使用者知情同意再開。
6. **升級 Blaze 方案的當下就順手設定「預算警示」（Budget Alerts）。** 雖然預期用量遠低於免費額度，但只要程式碼有一個環節寫錯（例如漏加終止條件的迴圈、或端點被盜用狂打），沒設警示的話很可能是帳單寄來才第一次發現，設定好之後接近額度上限就會主動收到通知。
7. **Cloud Functions 加基本的呼叫頻率限制（rate limiting）。** 避免單一帳號（不管是自己不小心連點還是被盜用）短時間內狂發請求，一部分是保護你的荷包，一部分是避免整個 Anthropic API 額度被少數異常請求用光，影響到其他家人的正常使用。
8. **知道 Anthropic 商用 API 的資料保留政策：** 預設不會拿你送出去的請求內容去訓練模型，但仍會有短期保留（用於防止濫用），這個等級的保留政策對記帳這種相對輕量的個資來說風險可接受，不需要特別做到企業級的「零資料保留」，但這件事本身要讓自己跟有登入的家人都知道，不要默默做掉。

---

## 四、技術債／內部改善（不影響使用者體感，但值得做）

- [ ] **CSV 匯入格式做成可擴充架構**：現在 `importMoneyNoteCsv()` 是寫死認 MoneyNote 格式，之後如果還要支援別的記帳 App（AndroMoney、CWMoney 等）建議抽成「格式偵測 + parser 註冊」的架構，不要每次都複製一份新函式
- [ ] **基本自動化測試**：目前所有驗證都是手動用 `javascript_tool` 操作 DOM 完成，沒有留下可重跑的測試檔。功能持續變多之後建議至少把核心的 `parseQuickInput`／`guessCategory`／CSV 解析邏輯抽出來寫成幾個純函式單元測試（不需要引入完整測試框架，一個簡單的 assert 腳本也夠用）
- [ ] **Service worker cache 版本化**：`sw.js` 的 `CACHE_NAME` 目前是手動改字串才會強制更新快取，可以自動化（例如用 build 時間戳）避免使用者卡在舊版本
- [ ] **色彩系統可及性複查**：`--pastel-*` 這組是使用者自選的裝飾色，沒有跑過完整的 CVD 驗證（`--series-*` 那組有），純色票夠用但如果之後圖表也要用到 pastel 色，要先過一次驗證
